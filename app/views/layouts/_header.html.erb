<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" 
              aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Client filtering", contacts_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if controller_name == 'contacts' && action_name == 'new' %>
          <li class="active">
        <% else %>
          <li>
        <%end%>
          <%= link_to t(:new_contact), new_contact_path %>
        </li>
        <% if controller_name == 'contacts' && action_name == 'index' %>
          <li class="active">
        <% else %>
          <li>
        <%end%>
          <%= link_to t(:contact_filter), contacts_path %>
        </li>
      </ul>
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>